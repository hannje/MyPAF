import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mypaf.app',
  appName: 'paf-system-frontend',
  webDir: 'build',
  bundleWebRuntime:false
  
};

export default config;
