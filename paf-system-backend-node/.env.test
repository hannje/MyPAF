# Test Environment Configuration
NODE_ENV=test
DB_HOST=localhost
DB_USER=test_user
DB_PASSWORD=test_password
DB_NAME=test_paf_db
SESSION_SECRET=test-session-secret-key-for-testing
PORT=3001
HTTPS_PORT=3443

# Email settings (mocked in tests)
MAIL_HOST=smtp.test.com
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=test@example.com
MAIL_PASS=test_password

# Redis settings (optional for tests)
REDIS_HOST=localhost
REDIS_PORT=6379